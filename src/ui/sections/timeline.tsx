import { RouterAnimation } from '../../animations/router-animation';

export const Timeline = () => {
  return (
    <RouterAnimation>
      Timeline.For this, we can mimic the design of roadmap.io with some
      animations
    </RouterAnimation>
  );
};
